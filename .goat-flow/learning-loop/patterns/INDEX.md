---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [named error class + action-handler wrapper for graceful user-facing errors](error-handling.md) (search: "## Pattern: named error class + action-handler wrapper for graceful user-facing errors") - Any CLI command that consumes user-controlled input (config files, baseline files, CLI flag values, file paths, etc.) can fail for two distinct reasons: the user provided something invalid, or there…
- [pair the perf harness with a warmed in-process profile](performance.md) (search: "## Pattern: pair the perf harness with a warmed in-process profile") - CLI-level runs include Node/tsx startup, which can hide scanner hot paths when the workload is small.
- [empirically reproduce multi-agent PR review claims before agreeing](pr-review.md) (search: "## Pattern: empirically reproduce multi-agent PR review claims before agreeing") - Bot-driven reviews (Copilot, Codex, CodeRabbit) often each leave a dozen comments on the same diff.
- [split a renderer module via a shared "rows" helper to break circular imports](refactoring.md) (search: "## Pattern: split a renderer module via a shared") - A renderer module (e.g.
- [preserve a JSON wire key while renaming the internal interface field](refactoring.md) (search: "## Pattern: preserve a JSON wire key while renaming the internal interface field") - `naming.boolean-prefix` fires on interface fields that lack an `is`/`has`/`can`/`should`/`will` prefix.
- ["version consistency" check verifies internal surfaces, not registry state](refactoring.md) (search: '## Pattern: "version consistency" check verifies internal surfaces, not registry state') - The preflight gate (`scripts/preflight-checks.sh`) historically ran two checks under one "Release version" heading: (a) lockstep across `package.json`, `package-lock.json`, `src/constants.ts`; and…
- [removing a rule from the gruff-ts catalogue](rule-catalogue.md) (search: "## Pattern: removing a rule from the gruff-ts catalogue") - the user asks to delete a rule (e.g.
- [preserving user customisations across config regeneration](rule-catalogue.md) (search: "## Pattern: preserving user customisations across config regeneration") - any flow that overwrites `.gruff-ts.yaml` from defaults (`gruff-ts init --force`, future migrate flows).
