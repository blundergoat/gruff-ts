---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [`/scan` mutates `process.cwd()` via `chdir()`](dashboard.md) (search: "## Footgun: `/scan` mutates `process.cwd()` via `chdir()`") - `startDashboard` (`src/dashboard.ts`, search: `function startDashboard`) handles `/scan` by…
- [`projectRoot` is read from query string and passed straight to `chdir`](dashboard.md) (search: "## Footgun: `projectRoot` is read from query string and passed straight to `chdir`") - `/scan?projectRoot=...` (in `src/dashboard.ts`, search: `url.searchParams.get("projectRoot")`)…
- [HTML output is concatenated, not templated](dashboard.md) (search: "## Footgun: HTML output is concatenated, not templated") - The `/` page and the `/scan` HTML response in `startDashboard` are built by string concatenation.
- [escaped backticks inside a single-backtick code span silently mangle markdown (and trip MD038)](docs-authoring.md) (search: "## Footgun: escaped backticks inside a single-backtick code span silently mangle markdown (and trip MD038)") - The learning-loop docs are dense with backtick anchors, so authors reach for backslash-escaped…
- [moving a `.goat-flow/` doc strands source comments that cite its old path](docs-authoring.md) (search: "## Footgun: moving a `.goat-flow/` doc strands source comments that cite its old path") - Source comments cite learning-loop docs by relative path.
- [generic Codex skill validation rejects required goat-flow version metadata](docs-authoring.md) (search: "## Footgun: generic Codex skill validation rejects required goat-flow version metadata") - The system `skill-creator/scripts/quick_validate.py` validator and goat-flow use different…
- [goat-flow upgrades can expose project-owned cold-path drift](docs-authoring.md) (search: "## Footgun: goat-flow upgrades can expose project-owned cold-path drift") - The 1.14.0 setup audit passed while `audit . --agent codex --harness` failed because existing…
- [`audit --agent <one>` never inspects the sibling agents' instruction files](docs-authoring.md) (search: "## Footgun: `audit --agent <one>` never inspects the sibling agents' instruction files") - This workspace keeps four near-identical agent instruction files (`CLAUDE.md`, `AGENTS.md`,…
- [gitignore-aware search finds nothing when recursion starts at `.goat-flow/`](docs-authoring.md) (search: "## Footgun: gitignore-aware search finds nothing when recursion starts at `.goat-flow/`") - `.goat-flow/.gitignore` (search: `# Ignore everything by default`) opens with a blanket `*` and…
- [a `files` allowlist in package.json bypasses root `.npmignore` inside listed dirs](packaging.md) (search: "## Footgun: a `files` allowlist in package.json bypasses root `.npmignore` inside listed dirs") - When `package.json` has a `files` array and that array names a directory (for example `"src/"` or…
- [`functionBlocks` is regex-based, not a TS AST](parser.md) (search: "## Footgun: `functionBlocks` is regex-based, not a TS AST") - `functionBlocks` (search: `function functionBlocks`) walks lines and matches one of four…
- [`parseDiagnostics` is syntax-only TypeScript parsing, not typechecking](parser.md) (search: "## Footgun: `parseDiagnostics` is syntax-only TypeScript parsing, not typechecking") - `parseDiagnostics` (`src/source-text.ts`, search: `function parseDiagnostics`) now uses…
- [`analyseDeadCode` private-method check is single-file only](parser.md) (search: "## Footgun: `analyseDeadCode` private-method check is single-file only") - `analyseDeadCode` (search: `function analyseDeadCode`) flags a `private` method as unused when its…
- [`analyseUnreachable` requires the terminating statement to end in `;`](parser.md) (search: "## Footgun: `analyseUnreachable` requires the terminating statement to end in `;`") - `analyseUnreachable` (search: `function analyseUnreachable`) only marks the previous line as…
- [nested template interpolation can mask the rest of a scanned file](rule-scanners.md) (search: "## Footgun: nested template interpolation can mask the rest of a scanned file") - `maskNonCode` (`src/source-text.ts`, search: `function maskNonCode`) tracks template interpolation…
- [line-rule emitters hardcode severity, so config `severity:` overrides are silently dropped](rule-scanners.md) (search: "## Footgun: line-rule emitters hardcode severity, so config `severity:` overrides are silently dropped") - There is no central pass that re-applies config severity to findings - each rule must consult…
- [`typeof x === "function"` is often runtime behavior, not code shape](rule-scanners.md) (search: "## Footgun: `typeof x ===") - `test-quality.static-analysis-redundant-test` (`src/test-block-rules.ts`, search: `function…
- [per-line walkers miss multi-line conditional context](rule-scanners.md) (search: "## Footgun: per-line walkers miss multi-line conditional context") - `analyseUnreachable` (`src/dead-code-rules.ts`, search: `function analyseUnreachable`) walks lines…
- [widening any rule's suppression criteria breaks coverage fixtures](rule-scanners.md) (search: "## Footgun: widening any rule's suppression criteria breaks coverage fixtures") - Every rule's suppression heuristic - whether a rationale-comment regex (M01), a fixture-loop…
- [rule descriptor threshold tests need literal rule ids](rule-scanners.md) (search: "## Footgun: rule descriptor threshold tests need literal rule ids") - `src/rule-catalogue.test.ts` extracts implementation defaults with regexes over source text:…
- [rule-descriptor prose triggers the rule it describes](rule-scanners.md) (search: "## Footgun: rule-descriptor prose triggers the rule it describes") - When M01 added a comment explaining the catch-rationale widening, the comment mentioned…
- [widening commented-out-code calls needs a prose-shape guard](rule-scanners.md) (search: "## Footgun: widening commented-out-code calls needs a prose-shape guard") - `isCommentedOutCode` (`src/findings-helpers.ts`, search: `function isDisabledCall`) used to require…
- [context-doc rules only see the LAST `//` line as the leading comment](rule-scanners.md) (search: "## Footgun: context-doc rules only see the LAST `//` line as the leading comment") - `leadingCommentForLine` (`src/comment-rules.ts`, search: `function leadingCommentForLine`)…
- [header-shape exemptions that key off the regex match prefix leak to sibling loop forms](rule-scanners.md) (search: "## Footgun: header-shape exemptions that key off the regex match prefix leak to sibling loop forms") - `VARIABLE_DECLARATIONS` (`src/line-rules.ts`, search: `const VARIABLE_DECLARATIONS`) captures the…
- [line-local `^export` detection misses re-exported declarations](rule-scanners.md) (search: "## Footgun: line-local `^export` detection misses re-exported declarations") - `functionBlockFromMatch` (`src/blocks.ts`, search: `function functionBlockFromMatch`) originally…
- [`naming.acronym-case` is file-wide, not per-symbol](rule-scanners.md) (search: "## Footgun: `naming.acronym-case` is file-wide, not per-symbol") - `naming.acronym-case` (`src/class-rules.ts`, search: `ruleId: "naming.acronym-case"`) fired when…
- [`CORRELATED_COMPLEXITY_RULE_IDS` is defined twice and must be edited in lockstep](rule-scanners.md) (search: "## Footgun: `CORRELATED_COMPLEXITY_RULE_IDS` is defined twice and must be edited in lockstep") - The complexity-cluster rule-id set lives in TWO files with identical literals: `src/scoring.ts`…
- [a removed rule id left in a comment trips `docs.stale-comment` unless the SAME line carries a historical marker](rule-scanners.md) (search: "## Footgun: a removed rule id left in a comment trips `docs.stale-comment` unless the SAME line carries a historical marker") - After removing a rule from the catalogue, any committed comment that still names the dotted id…
- [a milestone may name "new" dependency rules that already exist under different ids](rule-scanners.md) (search: "## Footgun: a milestone may name") - The M25 task list named three "new" hardened-dependency rule ids to add -…
- [caching a parsed AST by `SourceFile` identity goes stale when the object is reused](rule-scanners.md) (search: "## Footgun: caching a parsed AST by `SourceFile` identity goes stale when the object is reused") - The syntax-only parser adapter `getSourceFile` (`src/security-flow-rules.ts`, search: `function…
- [AST sink argument walkers descend into non-sink callback bodies](rule-scanners.md) (search: "## Footgun: AST sink argument walkers descend into non-sink callback bodies") - `taintedInput` (`src/security-flow-rules.ts`, search: `function taintedInput`) originally walked…
- [score value semantics and JSON field shape are different contracts](schema-and-cli.md) (search: "## Footgun: score value semantics and JSON field shape are different contracts") - `scoreReport` (`src/scoring.ts`, search: `function scoreReport`) owns both the public…
- [docs, milestone plans, and these footguns still point at the pre-split `src/cli.ts`](schema-and-cli.md) (search: "## Footgun: docs, milestone plans, and these footguns still point at the pre-split `src/cli.ts`") - `src/cli.ts` was split into focused modules and is now a 24-line shell, but durable docs kept…
- [routing migration-path reads through the strict schema validator silently clobbers user state](schema-and-cli.md) (search: "## Footgun: routing migration-path reads through the strict schema validator silently clobbers user state") - `readExistingPreservedConfig` (`src/init-config.ts`, search: `function…
- [catching only ConfigLoadError lets raw IO/parse errors escape the formatted error path](schema-and-cli.md) (search: "## Footgun: catching only ConfigLoadError lets raw IO/parse errors escape the formatted error path") - `runWithConfigErrorHandling` (`src/cli-program.ts`, search: `async function…
- [schema version strings are public contract](schema-and-cli.md) (search: "## Footgun: schema version strings are public contract") - Three string literals are part of the public output contract: `gruff.analysis.v2` (set in…
- [`exitFor` returns 2 on ANY diagnostic, regardless of `--fail-on`](schema-and-cli.md) (search: "## Footgun: `exitFor` returns 2 on ANY diagnostic, regardless of `--fail-on`") - `exitFor` (`src/scoring.ts`, search: `function exitFor`) returns `2` if `report.diagnostics.length…
- [`--no-baseline` and `--no-config` are CommanderJS auto-negations, not custom flags](schema-and-cli.md) (search: "## Footgun: `--no-baseline` and `--no-config` are CommanderJS auto-negations, not custom flags") - `normalizeOptions` (`src/cli-program.ts`, search: `function normalizeOptions`) reads…
- [default-ignored directories are hardcoded and lowercase-only](schema-and-cli.md) (search: "## Footgun: default-ignored directories are hardcoded and lowercase-only") - `isDefaultIgnoredDir` (`src/discovery.ts`, search: `function isDefaultIgnoredDir`) checks the FIRST…
- [`gruff-ts init --force` regenerates the whole YAML and can wipe user customisations](schema-and-cli.md) (search: "## Footgun: `gruff-ts init --force` regenerates the whole YAML and can wipe user customisations") - `writeDefaultConfig` (`src/init-config.ts`, search: `function writeDefaultConfig`) overwrites…
- [rule count and scanned-file-types are mirrored across many docs with no consistency test](schema-and-cli.md) (search: "## Footgun: rule count and scanned-file-types are mirrored across many docs with no consistency test") - Removing `size.stylesheet-length` (the CSS-scan removal) required hand-editing the rule count in…
- [discovery has two entry paths - the walk and the explicit-file short-circuit](schema-and-cli.md) (search: "## Footgun: discovery has two entry paths - the walk and the explicit-file short-circuit") - `discoverSourceInput` (`src/discovery.ts`) handles an explicit file operand by short-circuiting to…
- [changed-region diff scope is not the same as project context](schema-and-cli.md) (search: "## Footgun: changed-region diff scope is not the same as project context") - `analyse` (`src/analyser.ts`, search: `function analyse`) originally filtered `discovery.files`…
- [`check-ignore` answers both explicit-file and changed-file prefilter questions](schema-and-cli.md) (search: "## Footgun: `check-ignore` answers both explicit-file and changed-file prefilter questions") - `check-ignore` (`src/check-ignore.ts`, search: `function checkIgnore`) calls `classifyPathIgnore`…
- [pre-existing `M <config>` in git status at session start may already represent a customisation loss](schema-and-cli.md) (search: "## Footgun: pre-existing `M <config>` in git status at session start may already represent a customisation loss") - If a session starts with `M .gruff-ts.yaml` (or any other user-curated config) already in the…
- [`cli.ts` must keep using `parseAsync` because action handlers are async](schema-and-cli.md) (search: "## Footgun: `cli.ts` must keep using `parseAsync` because action handlers are async") - `src/cli.ts` (search: `buildProgram().parseAsync(argv)`) now awaits Commander's async…
- [`gruff-ts init` only guards against `.gruff-ts.yaml`, not the four-name precedence list](schema-and-cli.md) (search: "## Footgun: `gruff-ts init` only guards against `.gruff-ts.yaml`, not the four-name precedence list") - `writeDefaultConfig` (search: `function writeDefaultConfig`) calls `existsSync(join(projectRoot,…
- [`--format` argParser wiring is inconsistent across commands](schema-and-cli.md) (search: "## Footgun: `--format` argParser wiring is inconsistent across commands") - `src/cli-program.ts` defines `parseSummaryFormat` (search: `function parseSummaryFormat`) as a…
- [`shouldPromptForInit` gates on stdin/stderr TTY but not stdout](schema-and-cli.md) (search: "## Footgun: `shouldPromptForInit` gates on stdin/stderr TTY but not stdout") - `shouldPromptForInit` (search: `function shouldPromptForInit`) checks `context.isStdinTty` and…
- [the finding fingerprint embeds `line`, so a baseline keyed on it churns on pure code movement](schema-and-cli.md) (search: "## Footgun: the finding fingerprint embeds `line`, so a baseline keyed on it churns on pure code movement") - `makeFinding` (`src/findings.ts`, search: `const fingerprint = createHash`) hashes `[ruleId,…
- [`mapfile -t` over `node -e "console.log(arr.join('\n'))"` produces `[""]` when the array is empty](scripts.md) (search: "## Footgun: `mapfile -t` over `node -e") - Pattern that looks safe but isn't: a shell function shells out to `node -e…
- [secret scanners scan analyzer source and fixtures too](sensitive-data.md) (search: "## Footgun: secret scanners scan analyzer source and fixtures too") - `analyseSensitiveData` (`src/sensitive-data-rules.ts`, search: `function analyseSensitiveData`)…
