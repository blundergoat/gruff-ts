---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [`/scan` mutates `process.cwd()` via `chdir()`](dashboard.md) (search: "## Footgun: `/scan` mutates `process.cwd()` via `chdir()`") - `startDashboard` (`src/cli.ts`, search: `function startDashboard`) handles `/scan` by capturing `cwd()`, calling `chdir(root)`, running `analyse({...})`, and restoring the previous cwd in a `finally`.
- [`projectRoot` is read from query string and passed straight to `chdir`](dashboard.md) (search: "## Footgun: `projectRoot` is read from query string and passed straight to `chdir`") - `/scan?projectRoot=...` (in `src/cli.ts`, search: `url.searchParams.get("projectRoot")`) accepts any path the operator supplies and `chdir`s to it.
- [HTML output is concatenated, not templated](dashboard.md) (search: "## Footgun: HTML output is concatenated, not templated") - The `/` page and the `/scan` HTML response in `startDashboard` are built by string concatenation.
- [escaped backticks inside a single-backtick code span silently mangle markdown (and trip MD038)](docs-authoring.md) (search: "## Footgun: escaped backticks inside a single-backtick code span silently mangle markdown (and trip MD038)") - The learning-loop docs are dense with backtick anchors, so authors reach for backslash-escaped backticks when an anchor must show a literal backtick or a template-literal snippet.
- [moving a `.goat-flow/` doc strands source comments that cite its old path](docs-authoring.md) (search: "## Footgun: moving a `.goat-flow/` doc strands source comments that cite its old path") - Source comments cite learning-loop docs by relative path.
- [a `files` allowlist in package.json bypasses root `.npmignore` inside listed dirs](packaging.md) (search: "## Footgun: a `files` allowlist in package.json bypasses root `.npmignore` inside listed dirs") - When `package.json` has a `files` array and that array names a directory (for example `"src/"` or `"docs/"`), npm-packlist includes the whole directory and does not apply the root `.npmignore` to…
- [`functionBlocks` is regex-based, not a TS AST](parser.md) (search: "## Footgun: `functionBlocks` is regex-based, not a TS AST") - `functionBlocks` (search: `function functionBlocks`) walks lines and matches one of four hand-rolled patterns.
- [`parseDiagnostics` is syntax-only TypeScript parsing, not typechecking](parser.md) (search: "## Footgun: `parseDiagnostics` is syntax-only TypeScript parsing, not typechecking") - `parseDiagnostics` (`src/source-text.ts`, search: `function parseDiagnostics`) now uses `typescript.createSourceFile(...).parseDiagnostics` for script files and picks `ScriptKind` by extension.
- [`analyseDeadCode` private-method check is single-file only](parser.md) (search: "## Footgun: `analyseDeadCode` private-method check is single-file only") - `analyseDeadCode` (search: `function analyseDeadCode`) flags a `private` method as unused when its name appears `<= 1` times in the same source file.
- [`analyseUnreachable` requires the terminating statement to end in `;`](parser.md) (search: "## Footgun: `analyseUnreachable` requires the terminating statement to end in `;`") - `analyseUnreachable` (search: `function analyseUnreachable`) only marks the previous line as terminating when its trimmed form matches `/\b(return|throw|process\.exit)\b/` AND ends with `;`.
- [line-rule emitters hardcode severity, so config `severity:` overrides are silently dropped](rule-scanners.md) (search: "## Footgun: line-rule emitters hardcode severity, so config `severity:` overrides are silently dropped") - There is no central pass that re-applies config severity to findings - each rule must consult config itself via `ruleSeverity(config, ruleId, default)` (`src/config.ts`, search: `function…
- [`typeof x === "function"` is often runtime behavior, not code shape](rule-scanners.md) (search: "## Footgun: `typeof x ===") - `test-quality.static-analysis-redundant-test` (`src/test-block-rules.ts`, search: `function typeofFunctionAssertions`) must not treat every `typeof <identifier-or-member> === "function"` assertion as…
- [context-doc rules read ONLY the last `//` line above a declaration](rule-scanners.md) (search: "## Footgun: context-doc rules read ONLY the last `//` line above a declaration") - The context-doc rules - `docs.missing-error-behavior-doc`, `docs.missing-why-for-complex-code`, `docs.missing-side-effect-doc`, `docs.missing-invariant-doc` (`src/context-doc-rules.ts`, search:…
- [per-line walkers miss multi-line conditional context](rule-scanners.md) (search: "## Footgun: per-line walkers miss multi-line conditional context") - `analyseUnreachable` (`src/dead-code-rules.ts`, search: `function analyseUnreachable`) walks lines one at a time and originally tracked "previous line was a braceless conditional opener" as a single…
- [widening any rule's suppression criteria breaks coverage fixtures](rule-scanners.md) (search: "## Footgun: widening any rule's suppression criteria breaks coverage fixtures") - Every rule's suppression heuristic - whether a rationale-comment regex (M01), a fixture-loop iterable+body check (M03), or any other "this case isn't really a defect" gate - has at least two failure…
- [rule descriptor threshold tests need literal rule ids](rule-scanners.md) (search: "## Footgun: rule descriptor threshold tests need literal rule ids") - `src/rule-catalogue.test.ts` extracts implementation defaults with regexes over source text: `threshold(config, "rule.id", default)` and `optionNumber(config, "rule.id", "key", default)`.
- [rule-descriptor prose triggers the rule it describes](rule-scanners.md) (search: "## Footgun: rule-descriptor prose triggers the rule it describes") - When M01 added a comment explaining the catch-rationale widening, the comment mentioned `TODO`/`FIXME`/`XXX` to explain which markers were excluded - and `docs.todo-without-tracking` immediately…
- [`process-exec` matches `RegExp.exec` source text](rule-scanners.md) (search: "## Footgun: `process-exec` matches `RegExp.exec` source text") - `processExecCandidate` (`src/cli.ts`, search: `function processExecCandidate`) matches bare `exec(`, `spawn(`, or `execFile(` in masked code.
- [widening commented-out-code calls needs a prose-shape guard](rule-scanners.md) (search: "## Footgun: widening commented-out-code calls needs a prose-shape guard") - `isCommentedOutCode` (`src/findings-helpers.ts`, search: `function isDisabledCall`) used to require a semicolon for disabled calls, so `// cleanup()` and `// service.reset()` were false negatives in…
- [context-doc rules only see the LAST `//` line as the leading comment](rule-scanners.md) (search: "## Footgun: context-doc rules only see the LAST `//` line as the leading comment") - `leadingCommentForLine` (`src/comment-rules.ts`, search: `function leadingCommentForLine`) reverse-walks `comments[]` and returns the FIRST CommentRecord whose `endLine < declarationLine`.
- [header-shape exemptions that key off the regex match prefix leak to sibling loop forms](rule-scanners.md) (search: "## Footgun: header-shape exemptions that key off the regex match prefix leak to sibling loop forms") - `VARIABLE_DECLARATIONS` (`src/line-rules.ts`, search: `const VARIABLE_DECLARATIONS`) captures the binding name out of `const`, `let`, `for ( const`, and `for ( let` headers in one regex.
- [line-local `^export` detection misses re-exported declarations](rule-scanners.md) (search: "## Footgun: line-local `^export` detection misses re-exported declarations") - `functionBlockFromMatch` (`src/blocks.ts`, search: `function functionBlockFromMatch`) originally set `isExported: /^\s*export\b/.test(scan.codeLines[index])` - a line-local check on the declaration…
- [`naming.acronym-case` is file-wide, not per-symbol](rule-scanners.md) (search: "## Footgun: `naming.acronym-case` is file-wide, not per-symbol") - `naming.acronym-case` (`src/class-rules.ts`, search: `ruleId: "naming.acronym-case"`) fires when the same known acronym appears in more than one casing anywhere in a single source file.
- [`CORRELATED_COMPLEXITY_RULE_IDS` is defined twice and must be edited in lockstep](rule-scanners.md) (search: "## Footgun: `CORRELATED_COMPLEXITY_RULE_IDS` is defined twice and must be edited in lockstep") - The complexity-cluster rule-id set lives in TWO files with identical literals: `src/scoring.ts` (search: `const CORRELATED_COMPLEXITY_RULE_IDS`) drives penalty clustering, and…
- [a removed rule id left in a comment trips `docs.stale-comment` unless the SAME line carries a historical marker](rule-scanners.md) (search: "## Footgun: a removed rule id left in a comment trips `docs.stale-comment` unless the SAME line carries a historical marker") - After removing a rule from the catalogue, any committed comment that still names the dotted id (`pillar.name`) becomes an "unknown rule id" to `pushStaleRuleReferenceFindings`…
- [a milestone may name "new" dependency rules that already exist under different ids](rule-scanners.md) (search: "## Footgun: a milestone may name") - The M25 task list named three "new" hardened-dependency rule ids to add - `security.dependency-install-script`, `security.dependency-git-url-reference`, `security.dependency-unpinned-version` - but…
- [caching a parsed AST by `SourceFile` identity goes stale when the object is reused](rule-scanners.md) (search: "## Footgun: caching a parsed AST by `SourceFile` identity goes stale when the object is reused") - The syntax-only parser adapter `getSourceFile` (`src/security-flow-rules.ts`, search: `function getSourceFile`) was first written to memoise `ts.createSourceFile` output in a `WeakMap` keyed by the…
- [AST sink argument walkers descend into non-sink callback bodies](rule-scanners.md) (search: "## Footgun: AST sink argument walkers descend into non-sink callback bodies") - `taintedInput` (`src/security-flow-rules.ts`, search: `function taintedInput`) originally walked every node under every sink argument.
- [score value semantics and JSON field shape are different contracts](schema-and-cli.md) (search: "## Footgun: score value semantics and JSON field shape are different contracts") - `scoreReport` (`src/scoring.ts`, search: `function scoreReport`) owns both the public `gruff.analysis.v2` score object shape and the numeric semantics inside that shape.
- [docs, milestone plans, and these footguns still point at the pre-split `src/cli.ts`](schema-and-cli.md) (search: "## Footgun: docs, milestone plans, and these footguns still point at the pre-split `src/cli.ts`") - `src/cli.ts` was split into focused modules and is now a ~22-line shell, but many durable docs were never refreshed: they still say "in `src/cli.ts`" for symbols that moved, and still cite…
- [routing migration-path reads through the strict schema validator silently clobbers user state](schema-and-cli.md) (search: "## Footgun: routing migration-path reads through the strict schema validator silently clobbers user state") - `readExistingPreservedConfig` (`src/init-config.ts`, search: `function readExistingPreservedConfig`) originally called `loadConfig` to recover `paths.ignore` and `minimumSeverity` from the existing…
- [catching only ConfigLoadError lets raw IO/parse errors escape the formatted error path](schema-and-cli.md) (search: "## Footgun: catching only ConfigLoadError lets raw IO/parse errors escape the formatted error path") - `runWithConfigErrorHandling` (`src/cli-program.ts`, search: `async function runWithConfigErrorHandling`) catches `ConfigLoadError` and rethrows everything else.
- [schema version strings are public contract](schema-and-cli.md) (search: "## Footgun: schema version strings are public contract") - Three string literals are part of the public output contract: `gruff.analysis.v2` (set in `src/analyser.ts`:`analyse`, search: `schemaVersion: "gruff.analysis.v2"`), `gruff.baseline.v1`…
- [`exitFor` returns 2 on ANY diagnostic, regardless of `--fail-on`](schema-and-cli.md) (search: "## Footgun: `exitFor` returns 2 on ANY diagnostic, regardless of `--fail-on`") - `exitFor` (`src/cli.ts`, search: `function exitFor`) returns `2` if `report.diagnostics.length > 0` before it ever consults `failOn`.
- [`--no-baseline` and `--no-config` are CommanderJS auto-negations, not custom flags](schema-and-cli.md) (search: "## Footgun: `--no-baseline` and `--no-config` are CommanderJS auto-negations, not custom flags") - `normalizeOptions` (`src/cli.ts`, search: `function normalizeOptions`) reads `rawOptions.config === false` and `rawOptions.noConfig === true` to decide whether to load the default `.gruff-ts.yaml`;…
- [default-ignored directories are hardcoded and lowercase-only](schema-and-cli.md) (search: "## Footgun: default-ignored directories are hardcoded and lowercase-only") - `isDefaultIgnoredDir` (`src/cli.ts`, search: `function isDefaultIgnoredDir`) checks the FIRST path segment against a fixed lowercase list (`.git`, `.hg`, `.svn`, `.idea`, `.vscode`, `build`, `cache`,…
- [`gruff-ts init --force` regenerates the whole YAML and can wipe user customisations](schema-and-cli.md) (search: "## Footgun: `gruff-ts init --force` regenerates the whole YAML and can wipe user customisations") - `writeDefaultConfig` (`src/init-config.ts`, search: `function writeDefaultConfig`) overwrites `.gruff-ts.yaml` with the registry-derived default whenever `--force` is set.
- [rule count and scanned-file-types are mirrored across many docs with no consistency test](schema-and-cli.md) (search: "## Footgun: rule count and scanned-file-types are mirrored across many docs with no consistency test") - Removing `size.stylesheet-length` (the CSS-scan removal) required hand-editing the rule count in `package.json` description, `README.md` (catalogue row + the "contains N rules" line + the `size`…
- [discovery has two entry paths - the walk and the explicit-file short-circuit](schema-and-cli.md) (search: "## Footgun: discovery has two entry paths - the walk and the explicit-file short-circuit") - `discoverSourceInput` (`src/discovery.ts`) handles an explicit file operand by short-circuiting to `pushSourceFile` and returning BEFORE the directory `walk`.
- [changed-region diff scope is not the same as project context](schema-and-cli.md) (search: "## Footgun: changed-region diff scope is not the same as project context") - `analyse` (`src/analyser.ts`, search: `function analyse`) originally filtered `discovery.files` before scanning whenever `--diff` / `--since` produced a file/range scope.
- [`check-ignore` answers both explicit-file and changed-file prefilter questions](schema-and-cli.md) (search: "## Footgun: `check-ignore` answers both explicit-file and changed-file prefilter questions") - `check-ignore` (`src/check-ignore.ts`, search: `function checkIgnore`) calls `classifyPathIgnore` (`src/discovery.ts`) without knowing whether the caller is asking "would `analyse this-file.ts` scan…
- [pre-existing `M <config>` in git status at session start may already represent a customisation loss](schema-and-cli.md) (search: "## Footgun: pre-existing `M <config>` in git status at session start may already represent a customisation loss") - If a session starts with `M .gruff-ts.yaml` (or any other user-curated config) already in the working tree, do NOT treat that as "fine, the user is mid-edit." Run `git diff -- .gruff-ts.yaml` against…
- [`cli.ts` must keep using `parseAsync` because action handlers are async](schema-and-cli.md) (search: "## Footgun: `cli.ts` must keep using `parseAsync` because action handlers are async") - `src/cli.ts` (search: `buildProgram().parseAsync(argv)`) now awaits Commander's async `parseAsync()` inside an async IIFE, because action handlers in `src/cli-program.ts` for `analyse`, `summary`,…
- [`gruff-ts init` only guards against `.gruff-ts.yaml`, not the four-name precedence list](schema-and-cli.md) (search: "## Footgun: `gruff-ts init` only guards against `.gruff-ts.yaml`, not the four-name precedence list") - `writeDefaultConfig` (search: `function writeDefaultConfig`) calls `existsSync(join(projectRoot, DEFAULT_CONFIG_FILE_NAME))` to decide whether to refuse a write.
- [`--format` argParser wiring is inconsistent across commands](schema-and-cli.md) (search: "## Footgun: `--format` argParser wiring is inconsistent across commands") - `src/cli-program.ts` defines `parseSummaryFormat` (search: `function parseSummaryFormat`) as a Commander argParser that throws `InvalidArgumentError` on anything other than `text` or `json`.
- [`shouldPromptForInit` gates on stdin/stderr TTY but not stdout](schema-and-cli.md) (search: "## Footgun: `shouldPromptForInit` gates on stdin/stderr TTY but not stdout") - `shouldPromptForInit` (search: `function shouldPromptForInit`) checks `context.isStdinTty` and `context.isStderrTty`, but `InitPromptContext` (search: `interface InitPromptContext`) has no…
- [the finding fingerprint embeds `line`, so a baseline keyed on it churns on pure code movement](schema-and-cli.md) (search: "## Footgun: the finding fingerprint embeds `line`, so a baseline keyed on it churns on pure code movement") - `makeFinding` (`src/findings.ts`, search: `const fingerprint = createHash`) hashes `[ruleId, filePath, line, symbol]` into the 16-hex fingerprint, and `applyBaseline` (`src/baseline.ts`, search:…
- [`mapfile -t` over `node -e "console.log(arr.join('\n'))"` produces `[""]` when the array is empty](scripts.md) (search: "## Footgun: `mapfile -t` over `node -e") - Pattern that looks safe but isn't: a shell function shells out to `node -e "...console.log(Object.keys(x).join('\n'))"` and the caller reads the result with `mapfile -t arr < <(...)`.
- [secret scanners scan analyzer source and fixtures too](sensitive-data.md) (search: "## Footgun: secret scanners scan analyzer source and fixtures too") - `analyseSensitiveData` (`src/cli.ts`, search: `function analyseSensitiveData`) runs against every discovered text/config/TypeScript file, including `src/cli.ts`, `src/cli.test.ts`, fixtures, and…
