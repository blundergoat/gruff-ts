---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [restart browser-visible servers after source edits](smoke-verification.md) (search: "## Lesson: restart browser-visible servers after source edits") - During dashboard parity verification, screenshots were first captured against a dashboard server that had been started before the final `src/cli.ts` CSS tweak.
- [wait for post-interaction UI state, not just selectors](smoke-verification.md) (search: "## Lesson: wait for post-interaction UI state, not just selectors") - The dashboard-parity screenshot script initially clicked dashboard Refresh and then read `[data-scan-status]` before the iframe `load` handler had settled, producing a false failure with status…
- [update positive fixtures when raising rule thresholds](smoke-verification.md) (search: "## Lesson: update positive fixtures when raising rule thresholds") - The `test-quality.setup-bloat` default moved from 8 to 12 setup lines, but two positive fixtures still used only nine setup statements.
- [verification commands must account for local artifact directories](smoke-verification.md) (search: "## Lesson: verification commands must account for local artifact directories") - During the related-projects intake work, the clone inventory command originally listed every directory under `.goat-flow/scratchpad/related-projects`, but the task itself created…
- [widen typed test maps when one list has documented exceptions](smoke-verification.md) (search: "## Lesson: widen typed test maps when one list has documented exceptions") - During rule-quality doctrine work, the first `npm run check` failed in `tsc` because a rule-quality self-check built one `Map` from doctrine entries and another from exception entries.
- [keep fixture strings compact when fixing self-scan import noise](smoke-verification.md) (search: "## Lesson: keep fixture strings compact when fixing self-scan import noise") - While clearing an unused-import self-scan finding, expanding a template fixture into an array of string lines made the surrounding test exceed `test-quality.setup-bloat` and re-triggered…
- [self-scan CLI onboarding changes before close-out](smoke-verification.md) (search: "## Lesson: self-scan CLI onboarding changes before close-out") - During baseline-onboarding work, `npm run check` passed but `./bin/gruff-ts summary . --fail-on=none --no-baseline` exposed new gruff findings from newly added helper functions and a CLI test that…
- [baseline smoke tests must keep project root stable](smoke-verification.md) (search: "## Lesson: baseline smoke tests must keep project root stable") - A manual baseline smoke generated `gruff-baseline.json` from the repository cwd against an absolute `/tmp/.../sample.ts`, then tried to auto-apply it from the temp project cwd.
- [ground-truth a throwaway when a test result looks impossible (or the harness drops its output)](smoke-verification.md) (search: "## Lesson: ground-truth a throwaway when a test result looks impossible (or the harness drops its output)") - During the M25 AST-flow slice, `npm test` reported five failing security-flow tests whose failures were logically impossible from reading the code (a negative case with no sink "firing" a filesystem…
- [self-scan new CLI fixtures before settling their test file](verification.md) (search: "## Lesson: self-scan new CLI fixtures before settling their test file") - During the `security.new-function` config regression, the first CLI test landed in `src/cli.test.ts`, pushing that already-near-threshold file over `size.file-length`.
- [positive scanner fixtures and repo self-scan do not prove false-positive safety](verification.md) (search: "## Lesson: positive scanner fixtures and repo self-scan do not prove false-positive safety") - The first close-out for `test-quality.static-analysis-redundant-test` had `npm run check` green and a repo self-scan with zero hits, but an independent QA pass built a minimal runtime-callable repro…
- [converting the dogfood config to a profile breaks rule-enumeration contract tests](verification.md) (search: "## Lesson: converting the dogfood config to a profile breaks rule-enumeration contract tests") - After replacing the repo `.gruff-ts.yaml`'s flat 120-rule block with `profile: recommended` (the named-profiles dogfood step), `npm run check` went from 274/274 to 3 failures.
- [a near-budget file plus "add subsystem X here" forces an extraction](verification.md) (search: "## Lesson: a near-budget file plus") - The milestone said to add `resolveProfile` and the profile machinery to `src/config.ts`.
- [run git status before recommending commit, push, or PR steps](verification.md) (search: "## Lesson: run git status before recommending commit, push, or PR steps") - After finishing M06 verification, I answered "next best step: commit this verified work" from stale context instead of checking the current repository state.
- [self-scan freshly added regression tests before closing](verification.md) (search: "## Lesson: self-scan freshly added regression tests before closing") - The normal `npm run check` gate passed after the rubric-calibration tests were added, but the follow-up gruff self-scan found a `test-quality.magic-number-assertion` in the new regression test itself.
- [re-read changed files and contract wording after checks in an active workspace](verification.md) (search: "## Lesson: re-read changed files and contract wording after checks in an active workspace") - During the M06 rubric calibration pass, other agent work was active in the same checkout.
- [self-scan comment fixes need context-marker words](verification.md) (search: "## Lesson: self-scan comment fixes need context-marker words") - A first self-scan cleanup added leading comments and removed most findings, but the follow-up scan still reported context-doc gaps because the comments did not include the rule's expected contract,…
- [broadening scope can invalidate old suppression-count assertions](verification.md) (search: "## Lesson: broadening scope can invalidate old suppression-count assertions") - A review fix changed diff-scoped analysis from "scan changed files only" to "scan the full project, then filter emitted findings" so project-level rules keep central-test and import-graph context.
- [grep source for symbol locations; docs lag the cli.ts split](verification.md) (search: "## Lesson: grep source for symbol locations; docs lag the cli.ts split") - While auditing the 0.3.0 milestone plans, I claimed `exitFor` lives in `src/cli-program.ts`, copying `.goat-flow/architecture.md`.
- [run a no-diff control when a zero-tolerance perf gate fails](verification.md) (search: "## Lesson: run a no-diff control when a zero-tolerance perf gate fails") - During analyser performance work, two small hot-path patches were reverted after `scripts/test-performance.sh --matrix --baseline ... --fail-on-regression 0` reported regressions.
- [Codex permission audits reject absent exact workspace paths](verification.md) (search: "## Lesson: Codex permission audits reject absent exact workspace paths") - While fixing the Codex secret-path audit gate, switching `.codex/config.toml` to `:workspace_roots` made file-read deny coverage observable, but the next audit failed because exact workspace-root…
- [targeted self-scan fixes still need comment-quality review](verification.md) (search: "## Lesson: targeted self-scan fixes still need comment-quality review") - A self-scan cleanup first cleared `docs.missing-function-doc` and `docs.missing-interface-doc` by adding repetitive `Maintainer note:` comments.
- [verify extracted modules for circular self-scan edges](verification.md) (search: "## Lesson: verify extracted modules for circular self-scan edges") - During the first `src/cli-program.ts` extraction, the new module imported `analyse` directly from `src/cli.ts` while `src/cli.ts` imported `buildProgram` from `src/cli-program.ts`.
- [self-scan calibration should inspect the candidate class, not only targeted fixtures](verification.md) (search: "## Lesson: self-scan calibration should inspect the candidate class, not only targeted fixtures") - During fixture-purpose rule work, the focused tests passed after implementation, but the self-scan showed broad `test-setup` findings because `analyseProject(...)` alone was treated as a fixture…
- [self-scan refactors must account for rules that apply to new helpers](verification.md) (search: "## Lesson: self-scan refactors must account for rules that apply to new helpers") - During self-scan cleanup, the first `src/cli.ts` helper split removed unused-parameter and complexity findings but introduced new self-scan noise from undocumented helper functions, generic local…
- [exact optional properties must be omitted instead of set to undefined](verification.md) (search: "## Lesson: exact optional properties must be omitted instead of set to undefined") - During stale-comment rule work, `staleCommentFinding` initially passed `symbol: metadata.symbol` into `makeFinding`, which can be `undefined` at runtime.
- [use raw source for human labels and masked source only for code-position proof](verification.md) (search: "## Lesson: use raw source for human labels and masked source only for code-position proof") - During magic-threshold rule work, findings initially built labels from masked source text, so `threshold(config, "rule", "key", 55)` produced whitespace/dot labels after string masking.
- [line-anchored multiline regexes must not use `\s*` for indentation](verification.md) (search: "## Lesson: line-anchored multiline regexes must not use `\s*` for indentation") - During documentation-rubric work, `interfaceDeclarations` initially used a `gm` regex with `^\s*`, which let the indentation prefix consume newlines and report an interface at line 1 instead of its…
- [dynamic import is not a safe cycle breaker when the imported module re-enters the CLI](verification.md) (search: "## Lesson: dynamic import is not a safe cycle breaker when the imported module re-enters the CLI") - During self-scan cleanup, replacing the static dashboard import in `src/cli.ts` (`registerDashboardCommand`) with a dynamic import deadlocked the dashboard CLI because `src/dashboard.ts` imported…
- [benchmark workload labels need filesystem-safe temp names](verification.md) (search: "## Lesson: benchmark workload labels need filesystem-safe temp names") - During perf-harness verification, `./scripts/test-performance.sh --runs 1 --target fixtures/sample.ts --out /tmp/perf-spike.json` failed because the workload label `fixtures/sample.ts` was reused…
- [source-scanning contract tests must follow refactors across helper contexts](verification.md) (search: "## Lesson: source-scanning contract tests must follow refactors across helper contexts") - During self-scan cleanup, `npm run check` failed after threshold-backed rules were refactored to read thresholds through `context.config` instead of a direct `config` parameter.
- [keep verification wrappers visible to the deny hook](verification.md) (search: "## Lesson: keep verification wrappers visible to the deny hook") - The local deny-dangerous hook blocks verification wrappers that obscure nested execution.
- [zero-change refactor goldens come before edits](verification.md) (search: "## Lesson: zero-change refactor goldens come before edits") - During M06 hook pass reuse, the plan required the hook JSON golden matrix before implementation.
- [run `npm run check` after every `src/cli.ts` edit, not just before commit](verification.md) (search: "## Lesson: run `npm run check` after every `src/cli.ts` edit, not just before commit") - `src/cli.ts` is the entire runtime.
- [when changing rule output, regenerate the baseline test scenario, do not edit findings inline](verification.md) (search: "## Lesson: when changing rule output, regenerate the baseline test scenario, do not edit findings inline") - `src/cli.test.ts` writes a fixture and asserts that specific `ruleId`s appear (`security.eval-call`, `size.parameter-count`, `test-quality.no-assertions`, `modernisation.public-property`).
- [the deny-dangerous hook treats piping into `python3 -c`/`node -e` as blocked](verification.md) (search: "## Lesson: the deny-dangerous hook treats piping into `python3 -c`/`node -e` as blocked") - `.goat-flow/hooks/deny-dangerous.sh` blocks "pipe to interpreter" patterns.
- [threshold fixtures must exceed the threshold they are proving](verification.md) (search: "## Lesson: threshold fixtures must exceed the threshold they are proving") - The first high-entropy sensitive-data fixture initially used a 31-character secret-like value while the rule default required 32 characters, so the targeted test failed after implementation until the…
- [self-analysis smoke catches scanner regressions that fixtures miss](verification.md) (search: "## Lesson: self-analysis smoke catches scanner regressions that fixtures miss") - Core-expansion unit fixtures passed, but `./bin/gruff-ts analyse src --format=json --fail-on=none --no-config` exposed a `parse-error` in `src/cli.ts` and false positives where control statements…
- [inspect smoke output, not just exit status](verification.md) (search: "## Lesson: inspect smoke output, not just exit status") - Sensitive-data tests and the HTML smoke command exited 0, but the first HTML inspection showed obvious false positives on `package-lock.json` SRI hashes and the detector string `OPENAI_API_KEY`…
- [do not mutate the scanned tree during determinism checks](verification.md) (search: "## Lesson: do not mutate the scanned tree during determinism checks") - The determinism smoke was first run in parallel with baseline generation.
- [anchor repetitive fixture patches before trusting cumulative coverage](verification.md) (search: "## Lesson: anchor repetitive fixture patches before trusting cumulative coverage") - The cumulative rule-coverage test initially missed `test-quality.no-throw-only-test` because a patch matched the first `test("global mutation"` block in `src/cli.test.ts`, not the later cumulative…
- [rule-catalogue coverage fixtures must match scanner limits](verification.md) (search: "## Lesson: rule-catalogue coverage fixtures must match scanner limits") - The descriptor self-test first failed for `design.god-function` because the catalogue fixture was not long and complex enough, then failed for `test-quality.magic-number-assertion` because the…
- [milestone close-out must compare self-scan delta, not just `npm run check` pass count](workflow.md) (search: "## Lesson: milestone close-out must compare self-scan delta, not just `npm run check` pass count") - M01 and M02 both passed `npm run check` cleanly and shipped lock-in tests.
- [tick `- [ ]` checkboxes inside milestone files as work lands, do not just stamp `Status: complete`](workflow.md) (search: "## Lesson: tick `- [ ]` checkboxes inside milestone files as work lands, do not just stamp `Status: complete`") - Closed out the minimumSeverity track by setting `Status: complete (2026-05-27)` at the top of the 0.1.3 task files (M01-M03 + ISSUE.md, since archived).
- [moving milestone files across folders requires a cross-reference sweep, not just `git mv`](workflow.md) (search: "## Lesson: moving milestone files across folders requires a cross-reference sweep, not just `git mv`") - Asked to move the 0.1.3 task files {ISSUE,M01,M02,M03}.md into the 0.1.2 task folder plus delete `M09-rule-visibility-tier.md`.
- [user-facing CLI commands must catch known error classes and print graceful messages](workflow.md) (search: "## Lesson: user-facing CLI commands must catch known error classes and print graceful messages") - Shipped the `minimumSeverity` track with new config validators that threw plain `Error` instances when `.gruff-ts.yaml` was malformed.
- [run `git status` before describing the working-tree diff or proposing a commit message](workflow.md) (search: "## Lesson: run `git status` before describing the working-tree diff or proposing a commit message") - Operator asked "give me one line commit message for uncomitted code." Wrote a sweeping commit message claiming `0.1.2 ships rule-precision tier (M01-M08), minimumSeverity config schema, and graceful…
- [review pre-existing `M <file>` diffs before editing the same file](workflow.md) (search: "## Lesson: review pre-existing `M <file>` diffs before editing the same file") - Session started with `M .gruff-ts.yaml` in `gitStatus`.
- [never declare a release ready without auditing the active task folder](workflow.md) (search: "## Lesson: never declare a release ready without auditing the active task folder") - Operator asked "is this project ready to ship 0.1 version now?" The agent checked `package.json` version, ran `npm run check` (127/127 pass), self-scanned with `./bin/gruff-ts analyse .` (score…
